package practice;

import java.io.DataInputStream;
import java.io.FileInputStream;
import java.io.InputStream;

public class DateInputStream {
	public static int read3() {
		int sum = 0;
		FileInputStream fis = null;
		fis = new DataInputStream(new FileInputStream("data.txt"));
		DataInputStram di = new DataInputStream();
		InputStream
	}
}
